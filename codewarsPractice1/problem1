function makeNegative(num) {
  const isPositive = num >= 0;
  if(isPositive){
    // only positive numbers go through here
    return num * -1;
  } else {
    // only negative numbers go through here
    return num;
  }
}

console.log("return " + makeNegative(-5));
